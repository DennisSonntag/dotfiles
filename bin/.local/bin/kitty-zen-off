#!/bin/bash

hyprctl setprop address:$(hyprctl activewindow -j | jq '.address' | sed "s/\"//g") forcenoblur 0 lock
hyprctl dispatch fullscreen 1
kitty @ --to $KITTY_LISTEN_ON set-background-opacity 0.8 
